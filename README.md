Unity project testing the difference between for loops and foreach loops. 

In Unity editor, foreach is slighly faster, but on the device both loops get compiled to the same assembly, making them identical.
